# GetMacroRegExp (WantRefinementIndex)

## Возвращаемое значение

Значение String.

## Синтаксис

```
object.GetMacroRegExp (MacroName)
```

Метод GetMacroRegExp состоит из:

| Часть | Описание |
| --- | --- |
| объект | Объект типа [WantRefinementIndex](NormaCSAPI~WantRefinementIndex.md). |
| MacroName | Обязательный. Значение String. |
