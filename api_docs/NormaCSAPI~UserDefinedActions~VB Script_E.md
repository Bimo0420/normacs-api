# VB Script

## Исходный текст

Set app = CreateObject("NormaCS.Application") ' Читаем список надстроек из реестра 'app.UserDefinedActions.ReadForCurrentUser 'app.UserDefinedActions.ReadForAllUsers app.UserDefinedActions.ReadForCurrentUserOrForAllUsers ' Создаем пустую надстройку Set a = app.UserDefinedActions.CreateAction ' Заполняем реквизиты надстройки a.Command = "Notepad.exe" ' Команда a.Name = "Блокнот" ' Название a.Description = "Вызывает блокнот" ' Подсказка 'a.IsHidden = True ' Кнопка скрыта 'a.IsDocumentToolbar = True ' Кнопка на панели документа, а не на основной панели 'a.IconFilePath = "IconFilePath" ' Путь на файл с иображением 'a.IconIndex = 1 ' Индекс значка в файле 'a.Seq = 1000 ' Указывает, что кнопка должна быть первой среди подобных на данном тулюаре ' Добавляем к имеющемуся списку app.UserDefinedActions.Add(a) ' Сохраняем в реестре 'app.UserDefinedActions.SaveForAllUsersOrForCurrentUser 'app.UserDefinedActions.SaveForAllUsers app.UserDefinedActions.SaveForCurrentUser ' Снова читаем app.UserDefinedActions.ReadForCurrentUserOrForAllUsers ' Отображаем на консоли реквизиты надстроек For Each item In app.UserDefinedActions WScript.Echo "item.Command=" & item.Command WScript.Echo "item.Name=" & item.Name WScript.Echo "item.Description=" & item.Description WScript.Echo "item.IsHidden=" & item.IsHidden WScript.Echo "item.IsDocumentToolbar=" & item.IsDocumentToolbar WScript.Echo "item.IconFilePath=" & item.IconFilePath WScript.Echo "item.IconIndex=" & item.IconIndex WScript.Echo "item.Seq=" & item.Seq WScript.Echo "===" Next
