# GetMacroRegExp (UnambiguousIndex)

## Возвращаемое значение

Значение String.

## Синтаксис

```
object.GetMacroRegExp (MacroName)
```

Метод GetMacroRegExp состоит из:

| Часть | Описание |
| --- | --- |
| объект | Объект типа [UnambiguousIndex](NormaCSAPI~UnambiguousIndex.md). |
| MacroName | Обязательный. Значение String. |
