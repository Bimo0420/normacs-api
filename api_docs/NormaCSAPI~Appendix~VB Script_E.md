# VB Script

## Исходный текст

' Получаем ссылку на объект API Set app = CreateObject("NormaCS.Application") ' Получаем ссылку на объект документа Set doc = app.GetDocumentByURL("normacs://test.ru/1") ' Для следующих операций требуется доступ к NormaCS Pro ' Получаем ссылку на объект редактора NormaCS Pro. ' Выгрузка и редактирование приложений выполняются ' при помощи вызовов методов этого объекта. Set pro = app.domains("test.ru").GetNormaCSPro("1") ' Добавляем приложение с основным файлом и файлом предпросмотра. ' Указанные файлы будут загружены при выполнении этого вызова. ' Обратите внимание: для этой операции требуется подписка на дополнительную функцию "AllowUserAttach". pro.AddDocumentAppendix doc, "C:\Temp\test.docx", "Приложение1", "Первое приложение", "C:\Temp\test.pdf" ' Получаем ссылку на объект первого приложения в списке приложений документа. ' Вариант: Set apx = pro.AddDocumentAppendix(. . .) Set apx = doc.Appendixes(1) ' Выводим атрибуты приложения WScript.Echo "URL: " & apx.URL WScript.Echo "Name: " & apx.Name WScript.Echo "Comment: " & apx.Comment WScript.Echo "FileType: " & apx.FileType WScript.Echo "FileName: " & apx.FileName WScript.Echo "HasPreview: " & apx.HasPreview WScript.Echo "Id10: " & apx.Id10 WScript.Echo "Document URL: " & apx.Document.Url ' Выгружаем файлы приложения в папку C:\Temp pro.ExportDocumentAppendix doc, apx, "C:\Temp" pro.ExportDocumentAppendixPreview doc, apx, "C:\Temp" ' Обновляем приложение, указывая имя и описание приложения ' Обратите внимание: для этой операции требуется подписка на дополнительную функцию "AllowUserAttach". pro.ChangeDocumentAppendix doc, apx, "Приложение1 (изм.)", "Первое приложение (изм.)" ' Вариант: обновляем приложение, указывая имя и описание приложения, путь на основной файл ' и файл предпросмотра pro.ChangeDocumentAppendix doc, apx, "Приложение1 (изм.)", "Первое приложение (изм.)", "C:\Temp\test.docx", "C:\Temp\test.pdf" ' Вариант: обновляем приложение, указывая, что файл предпросмотра следует удалить pro.ChangeDocumentAppendix doc, apx, , , , "" ' Удаляем приложение pro.RemoveDocumentAppendix doc, apx
