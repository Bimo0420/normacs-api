# VB Script

## Исходный текст

' Получаем ссылку на объект API Set app = CreateObject("NormaCS.Application") ' Получаем ссылку на объект редактора NormaCS Pro Set pro = app.domains("test.ru").GetNormaCSPro("1") ' Задаем параметры для экспорта DestDir = "C:\temp" ' Папка экспорта DontExportDocumentFiles = False ' Не включать в пакет файлы документов ExportDocumentFilesNotOlderThen = Now - 30 ' Добавить только документы, внесенных в базу за последние 30 дней PlaceDocumentFilesInsidePack = False ' Поместить файлы документов в пакет обновления ' Для ускорения сборки использовать ранее сформированные для сборки файлы документов (в папке addonBase\\<домен>). ' Этот параметр не влияет на результат сборки. Не используется, если указан флаг PlaceDocumentFilesInsidePack. KeepExistedDocumentFiles = False IgnoreDocumentErrors = True ' Не прерывать обработку при ошибках, связанных с отдельными документами LogPath = "C:\temp\log.txt" ' Путь на лог ' Удаляем предыдущий лог, если был Set fso = WScript.CreateObject("Scripting.FileSystemObject") If fso.FileExists(LogPath) Then fso.DeleteFile LogPath End If ' Выполняем экспорт pro.ExportDomain _ DestDir, _ DontExportDocumentFiles, _ ExportDocumentFilesNotOlderThen, _ PlaceDocumentFilesInsidePack, _ KeepExistedDocumentFiles, _ IgnoreDocumentErrors, _ LogPath ' Открываем лог экспорта Set sho = WScript.CreateObject("WScript.Shell") sho.Run "notepad " & LogPath
