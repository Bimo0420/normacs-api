# URL Windows-моста через Cloudflare Tunnel
NORMACS_BRIDGE_URL=https://normacs-bridge.example.com

# Должен совпадать с BRIDGE_TOKEN в bridge/.env
NORMACS_BRIDGE_TOKEN=your-secret-token-here

# CORS: через запятую, без пробелов
CORS_ORIGINS=http://localhost:5173,https://yourdomain.com
